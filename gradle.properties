# Enable AndroidX support for all dependencies (modern Jetpack libraries require this)
android.useAndroidX=true

# Automatically convert third-party libraries to use AndroidX if necessary
android.enableJetifier=true

# Gradle daemon JVM configuration
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
